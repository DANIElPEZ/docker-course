version: '4.46.0'
services:
  backend:
    image: backend
    build: 
      context: ./backend
      dockerfile: dockerfile
    ports:
      - 5000:5000
  frontend:
    image: frontend
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - 8080:80
    depends_on:
      - backend